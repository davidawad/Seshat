/**
 * Applies the user's theme/typeface/motion settings to the document.
 * Placeholder — the typography/legibility track implements this fully
 * (e.g. setting data-theme / data-typeface attributes on <html> read from
 * useSeshatStore().state.settings, respecting prefers-reduced-motion).
 */
export const useApplyTheme = (): void => {}
